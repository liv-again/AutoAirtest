# AutoAirtest 操作手册

## 环境准备

### 硬件要求

- 一台 Windows PC
- 一台 Android 真机（Android 10+），已开启 USB 调试
- USB 数据线

### 软件依赖

```powershell
# Python 3.11+
python --version

# 安装依赖
pip install airtest pocoui openpyxl pyyaml requests
```

### 连接设备

```powershell
# 确认设备已连接
adb devices
# 输出示例: c923178d  device
```

## 快速开始

### 1. 准备配置文件

```powershell
# 复制模板配置
copy config-模板.yaml config.yaml
```

编辑 `config.yaml`，填入你的 API Key 和设备序列号：

```yaml
llm:
  api_key: "你的API_KEY"      # DeepSeek API Key
  model: deepseek-v4-pro

device:
  adb_serial: "你的设备序列号"  # adb devices 看到的序列号

input:
  excel_path: docs/test_1.xlsx  # 测试用例 Excel 路径
  sheet_name: 需求测试报告        # Sheet 名称
```

### 2. 准备测试用例 Excel

在 `docs/` 目录下放置测试用例 Excel，格式如下：

| 业务模块 | 功能模块 | 功能项 | 测试目的 | TC_用例名称 | 优先级 | 操作描述 | 预期结果 |
|----------|----------|--------|----------|-------------|--------|----------|----------|
| 交易 | 沪深A股 | 沪深A股 | 数据校验 | TC_上证A股列表数据正确 | middle | 交易-沪深A股-沪深A股 | 跳转到沪深A股页面，正确显示沪深A股列表 |

### 3. 运行

```powershell
# 使用配置文件运行（默认读取 config.yaml）
python -m autoairtest run

# 指定设备和 Excel
python -m autoairtest run --adb-serial c923178d --excel docs/test_1.xlsx --sheet 需求测试报告

# 按关键字筛选用例
python -m autoairtest run --case-filter 国内指数
```

### 4. 查看结果

运行结束后，结果保存在 `runs/<时间戳>/` 目录下：

```
runs/20260726-135251-206777_case/
├── report.html              # HTML 报告（双击打开）
├── run_summary.json         # 运行摘要
├── session_meta.json        # 会话元数据
├── cases/
│   └── TC_xxx/
│       ├── execution_plan.json      # 执行计划
│       ├── action_results.json      # 动作结果
│       ├── verification_result.json # 验证结果
│       ├── screenshots/             # 截图
│       └── element_summaries/       # Poco 控件树
```

## 配置说明

```yaml
llm:
  enabled: true                # 是否启用 LLM
  use_for_planning: true       # LLM 参与计划生成
  use_for_verification: true   # LLM 参与验证判定
  model: deepseek-v4-pro       # 模型名称
  temperature: 0.1             # 温度（越低越确定）
  timeout_seconds: 60          # 请求超时

execution:
  mode: device                 # device=真机, offline=离线模拟

input:
  excel_path: docs/test_1.xlsx # 测试用例文件
  sheet_name: 需求测试报告       # Sheet 名称
  case_filter: ""              # 用例筛选关键字
```

## 导航菜单配置

菜单结构定义在 `skills/navigation/nodes.yaml`，按 `中原证券菜单结构.md` 维护。

### 节点定义格式

```yaml
nodes:
  sh_a_stock:                  # 节点 ID
    text: 上证A股               # 界面显示文案
    parent: market_other        # 父节点 ID
    aliases:                    # 别名（业务术语映射）
      - 沪深A股
    children: []                # 子节点列表
```

### 重要规则

- **模块名不作为点击节点**：页面上的"个股""基金""债券"等模块标签不可点击，不应出现在导航路径中
- **别名映射**：`securities_navigation/aliases.yaml` 负责业务术语到界面文本的转换
- **添加新节点**：在 `nodes.yaml` 中新增节点后，需设置正确的 `parent` 和 `text`

## 命令行参考

```
python -m autoairtest <command>

命令:
  init-config    生成配置模板
  doctor         环境检查
  run            执行测试

run 参数:
  --config CONFIG              配置文件路径
  --excel EXCEL                Excel 路径
  --sheet SHEET                Sheet 名称
  --case-filter FILTER         用例筛选
  --adb-serial SERIAL          设备序列号
  --output-dir DIR             输出目录
  --execution-mode offline|device  执行模式
  --write-back-excel           输出带结果的 Excel
```

## 运行流程

```
1. 加载配置 → 2. 读取 Excel → 3. LLM 规划 → 4. 设备执行 → 5. 验证 → 6. 报告
```

### 规划阶段

- LLM 根据操作描述和预期结果生成执行计划和验证目标
- 导航路径由 `skills/navigation/nodes.yaml` 决定，LLM 不参与导航生成
- LLM 只负责生成验证类动作（observe/tap）

### 执行阶段

- 首个用例：启动 App → 按导航路径点击 → 执行验证动作
- 后续用例：navigate_home 返回首页 → 直接在当前 App 内继续导航
- 导航动作点击后页面无变化时视为成功（不阻塞）

### 验证阶段

- `element_order`：检查实体在页面中的相对顺序
- `data_display_completeness`：检查实体附近是否有数值数据
- `page_navigation`：检查目标文本是否出现在页面中
- `cross_page_consistency`：跨页面实体一致（MVP 中需人工复核）

## 常见问题

### Poco 连接失败

```
症状: 日志中反复出现 am instrument 重试
原因: Android 16 (SDK 36) 设备上 PocoService 兼容性问题
解决: 使用 Android 10-14 设备
```

### 动作 touch 坐标错误

```
症状: airtest touch failed: Coordinates must be a tuple
原因: poco_semantic locator 返回的坐标格式不正确
当前状态: 待修复
```

### LLM API 调用失败

```
检查: config.yaml 中 api_key 是否有效
检查: model 是否为 deepseek-v4-pro
测试: python -c "import requests; r=requests.post('https://api.deepseek.com/...')"
```

### 设备未授权

```powershell
adb kill-server
adb devices
# 手机上点击"允许 USB 调试"
```

## 项目结构

```
AutoAirtest/
├── autoairtest/           # 核心代码
│   ├── planning/          # LLM 规划器 + 规则规划器
│   ├── execution/         # 设备执行工作流
│   ├── verification/      # 验证引擎
│   ├── models.py          # 数据模型
│   ├── orchestrator.py    # 主控编排器
│   └── config.py          # 配置管理
├── skills/                # 项目技能
│   ├── navigation/        # 菜单导航节点
│   ├── securities_navigation/  # 证券术语别名
│   ├── expected_result_rules/  # 验证规则
│   └── stock_detail/      # 个股详情页元素
├── prompts/               # LLM Prompt
├── docs/                  # 文档和测试 Excel
├── runs/                  # 运行产物
└── config-模板.yaml       # 配置模板
```
